# Node / TypeScript
node_modules/
dist/
build/
*.tgz
*.tsbuildinfo

# Test + dev state
quota_state.json
test_state.json
coverage/
.vitest-cache/
.harness-dispatch/
.harness-router/

# Local tool state
.nvx/
.playwright-mcp/
.superpowers/

# Personal live config (may contain secrets/local overrides) — the shipped
# canonical default config the router loads and users copy/edit is
# config.default.yaml, tracked, a separate file with its own lifecycle so
# package updates to it never collide with a user's local instance.
config.yaml
LAUNCH-PLAN.md
BILLING_AUTH_AUDIT.md
R1-HANDOFF.md
HANDOVER.md

# Editor + OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Claude Code (settings and worktrees)
.claude/

# Backup files
*.bak

# Python archive remnants (safe to delete once archive/python-v1 is the only Python source)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
pytest-cache-files-*/
