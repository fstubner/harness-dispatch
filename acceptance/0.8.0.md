# Acceptance — 0.8.0

- version: 0.8.0
- verdict: CONDITIONAL
- date: 2026-08-29
- reviewer: independent agent passes, separate context from the build; entries
  marked BUILDER are the author's own self-assessment and were not independently
  reviewed at the time they were written

**Read the reviewer line literally.** A ninth pass pointed out that this record
was signing builder self-certification under an "independent" header — three
entries said "FIXED after the eighth pass" for fixes written by the code's
author, after the record commit, amended into it. That is the pattern the gate
exists to distrust, one level up from where it was looking. Those entries are
now marked, and the ninth pass verified all three: the redaction holds on every
surface it could reach, the config warnings fire for all three route shapes, and
the duplicate-name warning is correct with behaviour unchanged.

Six passes ran during this cycle. Each reviewed a commit, found defects, and the
fixes landed after it — so for most of them the reviewed state was not the state
that would ship. A seventh pass ran against `f564a10` specifically to close that
gap: the release candidate itself.

An eighth pass then audited THIS RECORD and the two fixes the seventh prompted,
which no one had reviewed. It corrected the record in three ways, recorded here
because a wrong record is a defect in its own right:

- Both previously-unreviewed fixes are now proven by RUNNING rather than by
  reading. The file-permission fix was verified on real POSIX (a `node:22-alpine`
  container, `umask 022`, driving the actual `connect` path): an 0600 config
  stays 0600, and a sabotage control that removes the mode carry produces 0644.
  The worktree cleanup was verified in both branches, with the git failure
  genuinely forced via `git worktree lock`.
- The claim that config warnings are reachable only from `doctor` was WRONG when
  written. `status` prints a `Config warnings (N)` block in both text and JSON,
  and `configure` prints them too. Only the `Unknown service` message still
  points at nothing. Removed from the open list.
- Neither of those two fixes shipped with a test, in a commit whose subject said
  `docs(acceptance)`. Both now have tests, each sabotage-checked.

## Open at ship time

### Post-release: the eighteenth pass (against the tag)

The first pass able to return an uncapped verdict, run against `v0.8.0` and
the published npm tarball. Two results worth recording:

**The tarball IS the tag.** `npm pack harness-dispatch@0.8.0` unpacked and
compared against a fresh build of a worktree at `v0.8.0`: byte-identical
modulo CRLF. Nobody had ever checked this; it is the check that matters most,
because the tarball is what users execute.

**Verdict CONDITIONAL, by judgement rather than by cap.** The mechanical gate
returned SHIP (all eight checks pass, `A-intent-anchored` green on the
human-signed PRODUCT.md). The acceptor overrode it, naming three findings that
contradict the product contract — "a SHIP with those as caveats would be a
CONDITIONAL wearing the wrong label". That is the gate working as designed for
the first time in this project's history.

Two of the three are FIXED (BUILDER) post-release, each sabotage-checked:
`execute` routed to endpoints that structurally cannot execute and reported
success, and an orphaned job returning nothing while its progress sat on disk
(both the `job_status` surface and the chaining preamble). The third — HTTP fanout arms not being
job-backed — is now FIXED too, on a second look prompted by Felix.

That correction is worth recording as its own lesson. It was first deferred on
the reasoning that "making it job-backed changes what an OpenAI-compatible
client receives". Reading the MCP fanout properly showed that is false: MCP
awaits each arm's completion and returns ordinary rows, so job-backing and the
response contract are independent concerns. The HTTP response is byte-identical
to before; only a `jobId` was added per row, additively, inside the
`harness_dispatch` extension namespace. A deferral justified by an assumption
nobody checked is the same defect class this record is full of — asserted
rather than verified — committed in a decision instead of in code.

Note on the first fix's blast radius: it broke one existing test whose CONTROL
probed eligibility with `taskType: execute`. The control's own comment says it
exists so the main assertion cannot pass for the wrong reason — so it was
moved to `plan` (which an endpoint legitimately serves) rather than deleted,
preserving exactly the property it was checking.


- `status --json` and the `harness-dispatch://status.json` MCP resource emitted
  each route's `base_url` verbatim, twice, credentials included — measured with
  a key in the URL, which is Google AI Studio's own shape. That resource is one
  this server's instructions tell agents to read, so the credential lands in an
  agent's context. The text surface was always redacted, which is why an earlier
  pass recorded "endpoint redaction — verified". **FIXED after the eighth pass
  (BUILDER)** — the ninth pass then verified it holds on every surface it could
  reach: CLI JSON and text, both MCP resources, HTTP `/v1/status` and
  `/v1/usage`, the dispatch log, and the endpoint-failure error path.
- A duplicated route name silently discards the first entry's
  `safety_profile` and `workspace_policy`. Measured: the surviving route ran
  `safety=workspace_edit workspace=shared_locked` where `read_only`/`copy` was
  written, with no warning on any surface. Silently less restrictive. **FIXED
  after the eighth pass (BUILDER)** — the ninth pass confirmed the warning names
  both fields and that the behaviour is still last-wins, as intended.

  Still open: a name collision ACROSS blocks (the same name in `clis:` and
  `endpoints:`) is not detected, because the check runs once per block. The
  result is not last-wins but a hybrid — the ninth pass measured the survivor
  taking the endpoint's type and base_url while keeping the CLI entry's
  `safety_profile` and silently dropping its `workspace_policy`.
- Wrong-typed values on RECOGNISED config keys are silently coerced to defaults
  with no warning — the warning machinery covers unknown keys only. `tier:
  metered` becomes tier 3, `weight: very-high` becomes 1.0, `disabled:` as a
  string is ignored, and `clis:`/`endpoints:` written as a map are dropped.
  Live on the maintainer's own machine today: four routes carry `tier: metered`
  and have silently been running at tier 3. **FIXED after the eighth pass (BUILDER; verified by the ninth)** for
  numeric and boolean fields — verified against that real config, which now
  names all four routes. `disabled:` written as a string, and `clis:`/
  `endpoints:` written as a map, remain silent and are still open.

  A delegate audit from the same pass also found the legacy `services:` shape
  getting NEITHER validator — both were wired into the two modern loops only, so
  a legacy config reported one warning where the modern shape reports four. The
  value check now runs there too. (The unknown-KEY check deliberately does not:
  the legacy shape accepts a wider set of keys, and reporting those as typos
  would be worse than the silence it replaces.)
- An empty HTTP 200 from an endpoint is reported as a SUCCESSFUL empty answer on
  the MCP surface and as a failure on the CLI. The buffered path guards on
  unextractable content; the streaming path treats zero parsed chunks with no
  stream error as success, and `jobs.ts` always uses the streaming path — so the
  MCP tool, which is the primary interface and the one an orchestrating agent
  branches on, is the one that gets it wrong. The breaker heals on it, so a route
  serving nothing but empty 200s never trips. Reproduced by the ninth pass.
  FIXED (BUILDER), after a first attempt that was wrong about its own premise.
  The tenth pass found the fix, its comment, the CHANGELOG and this entry all
  asserting that the buffered path already refused an empty answer — it did
  not. `#extractContent` returned any string, so a well-formed 200 with
  `content: ""` was a success with no output on the CLI path, measured as
  `dispatch` printing nothing and exiting 0. The claim had never been checked.
  Both paths now refuse it, verified in both directions rather than assumed in
  one. The tenth pass also found the new streaming error asserting "200 with no
  content" for bodies that DID carry content (an HTML error page, prose, and a
  gateway that ignored `stream: true`); it now names the body the way the
  buffered path does, while a genuinely empty SSE stream still reports no
  content. Sabotage-checked per guard.

  THIRD ATTEMPT, after the eleventh pass falsified the second. Guessing WHY a
  response was unusable was wrong three times in three different ways — the
  SSE test threw away a real answer arriving in another dialect, called a real
  provider's keepalives an unexpected shape, and classified the same body
  differently depending on how the network split it (the "bounded to 300
  characters" comment was false; it grew by whole chunks). The dispatcher no
  longer classifies: bytes arrived or they did not, and if they did they are
  quoted. The buffered path's dangling "Unexpected response shape: " for an
  empty body is gone too.

  The claim that the two paths therefore agreed was STILL not true, and the
  twelfth pass found the remaining case against a real socket: a server
  sending 200 plus a partial body and then resetting made the buffered path
  assert "the endpoint returned 200 with no body", while streaming reported
  "terminated" and kept the partial output. A failed read is not evidence of
  what the server sent. Both paths now call one shared function, so the
  agreement is enforced rather than claimed, and a failed read is reported as
  one. The message also no longer calls the shape unexpected — a stream that
  carried nothing had exactly the expected shape — and the CHANGELOG sentence
  asserting the previous version "cannot be wrong" is gone. Not re-verified by
  an independent pass.
- A corrupt or unreadable circuit-breaker record renders identically to a
  healthy route: `breaker=closed failures=0`, route Ready, nothing said by
  `status` or `doctor`. A single corrupt file silently un-trips a route
  mid-cooldown, which is what the persistence layer was added to prevent.
  Reproduced by the ninth pass. FIXED (BUILDER), with three stated limits.

  The lost failure count is NOT recovered and the route IS un-tripped — nothing
  on disk can say what it was. What changed is that this stops being silent.
  Failing closed was considered and rejected: it would strand a route until
  someone deleted a file by hand.

  `doctor` still says nothing; only `status` reports it, in the text and as
  `breaker.stateUnreadable` in the JSON. The issue above named both surfaces,
  so this entry is narrower than the defect it answers.

  The report is transient: the next event on that route overwrites the record,
  so an operator who does not run `status` in between never learns it happened.

  The first fix covered only a JSON parse failure. The tenth pass found a
  record that PARSES with wrong-typed fields still coercing to healthy in
  silence — `{"failures":"5","blockedUntilMs":"2099-01-01"}`, an active
  cooldown, read back as `failures: 0` — so this entry read as closed while
  half the defect was live. Both shapes are covered now; an absent field is
  still tolerated, since older builds wrote fewer of them. Sabotage-checked end
  to end, from a bad record through Router to `renderStatusText`, and once per
  type guard — the first version of that test set two wrong fields at once and
  passed with one guard deleted.

  The type-guard list was still incomplete: the eleventh pass found `[]`, `{}`
  and a foreign nested schema all reading back healthy in silence. Enumerating
  corruption was the wrong approach, and is now backed by the invariant the
  module already holds — a healthy route has no file, because a healthy save
  deletes one, so a file reading back healthy is a contradiction regardless of
  its shape. `doctor` is still silent and the report is still transient; both
  limits above stand, and the twelfth pass confirmed both descriptions accurate
  against the code. Not re-verified by an independent pass.
- `safeEqual` in `src/auth.ts` ran its length-mismatch comparison against the
  CALLER-SUPPLIED buffer, so the work scaled with a length an attacker chose,
  while the comment claimed "a length mismatch alone doesn't leak timing info
  either" — a property the code did not have. It now compares the expected
  buffer against itself, so every wrong length costs the same. Found by the
  twelfth pass from reading; negligible in practice and NOT reproduced as an
  exploitable signal, and this fix is not covered by a test because the
  property is a timing one and the boolean result was always correct.
  FIXED (BUILDER).
- `isAuthorized(header, null)` returns true — no token configured means every
  request is authorized. Unreachable through the HTTP server, which calls
  `ensureHttpToken()` at startup and falls back to the token read from disk on
  refresh, and it is covered by a test asserting exactly this. Left as-is
  because it is the documented behaviour of a no-auth mode rather than a
  defect, but the one-line branch read as a guard clause; it now carries a
  doc comment saying it fails open on purpose. NOT a behaviour change.
- The CLI aborts with a native libuv assertion and exits **127** on a routed
  fallback — a primary endpoint that answers 200 with a bad shape, then a
  working secondary. `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING),
  src\win\async.c, line 76`. Reproduced 5/5 by the tenth pass, on Windows /
  Node 24.14.1 only. It needs a primary that actually ANSWERED: a refused
  connection exits 1 cleanly, and `--service … --no-fallback` exits 1 cleanly.
  127 means "command not found" to any calling shell, and the process aborts
  rather than exits. Believed pre-existing rather than introduced by the
  empty-answer work — the CLI takes the untouched buffered path — but NOT
  confirmed against `main`, and that confirmation is the first thing to do.
  Also visible in the same run: the `dispatch:` line names only the surviving
  route, so the primary's failure is invisible in CLI output. NOT fixed.

  CONFIRMED PRE-EXISTING by the eleventh pass, which built `bd5b7c7` — before
  any of the empty-answer work — in a worktree and reproduced it 3/3 there,
  and again at `dc3c239` and on the branch. The earlier "believed … but NOT
  confirmed" is settled. Still NOT fixed, and still a real defect: the process
  aborts AFTER printing a correct answer, and a shell reads 127 as "command
  not found".
- A corrupt LEGACY `breaker_state.json` blob is caught, migrated as nothing,
  and then deleted, with a comment saying nothing is lost by deleting it.
  Recorded from reading only; the eleventh pass REPRODUCED it, and found it
  wider than described — the migration had none of the per-record type checks
  either, so a legacy entry holding a live cooldown in a shape it could not
  read was coerced to healthy, skipped, and destroyed with the file. FIXED
  (BUILDER), on the second attempt. The first shared the validator but not the
  floor: with no per-route "healthy is a contradiction" rule available here
  (the old format wrote healthy entries legitimately), `{}` and a foreign
  schema still coerced to healthy and were still destroyed — two of the three
  shapes the CHANGELOG claimed were now caught. The twelfth pass measured that
  against the built artifact, including with the snake_case shape of the
  Python implementation this module's own header says it was ported from. The
  shared validator now refuses a record naming none of the fields it
  understands.

  That first attempt also introduced a REGRESSION, found by the same pass:
  keeping the whole blob when any entry was bad meant the good entries were
  replayed on every read, so a route that had recovered had its deleted record
  recreated from stale state — left one failure from tripping, unable to heal,
  with nothing naming the blob. The blob is now rewritten with only what could
  not be consumed. Sabotage-checked per guard. Not re-verified by an
  independent pass.
- A fanout whose candidates are all policy-blocked returned `completed: true,
  results: []` — vacuously true over an empty array, on the field the tool
  description tells agents to branch on. FIXED by the fourteenth pass's round
  (see below), and measured fixed by the fifteenth: it now errors, naming each
  route and why. This bullet still read "NOT fixed" while a later bullet in
  this same file recorded the fix — the record contradicted itself for a day.
- THIRTEENTH PASS — the first to cover workspace policies, job control, argv
  safety, hot-reload and `anthropic_messages`. Twelve passes had concentrated on
  two files. Fixed (BUILDER), each sabotage-checked on its own:
  `workspace apply` destroying a committed file when the agent CREATED one at
  the same path (the conflict check skipped every change with no recorded base,
  which is every added file — the protection was absent for a whole change
  kind); a pinned `effective_safety` returning before the flag check, so a route
  with no read_only flags ran unconstrained while reporting `read_only` and
  wrote a file under a read-only dispatch; a `copy` workspace omitting
  `EXCLUDED_DIRS` with no mention anywhere; the documented in-project
  `HARNESS_DISPATCH_WORKSPACES_DIR` making every apply refuse on a clean tree;
  a failed hot-reload invisible to every client; `spawn git ENOENT` as a
  user-facing message; and `safeEqual` timing work scaled by attacker-chosen
  length.
- The `workspace` tool had NO flow in `ux-walkthrough.md` — the resolve half of
  isolated dispatch, and the only destructive operation in the product. Three
  defects that destroy or hide a user's work lived there, and a
  walkthrough-driven pass would have exercised none of them. Flow 6 added,
  covering the create-collision, double-apply, in-project workspaces root and
  missing-git cases specifically. This is the likeliest reason those defects
  survived twelve passes, so the gap is recorded as a finding in its own right.
- FOURTEENTH PASS — fanout, quota accounting, `doctor`/`configure`/`connect`,
  the supervisor pool under load, and auth token rotation, none of which had
  been examined. Fixed (BUILDER), each sabotage-checked on its own: a
  remaining-only rate-limit header nulling the known limit and returning the
  quota score to 1.0 (the only quota layer that reaches routing); an unreadable
  `quota_state.json` being REPLACED rather than skipped, destroying every
  route's history; an all-blocked fanout answering `completed: true` over zero
  arms; and `connect` spreading a non-object JSON root into an object and
  reporting success.

  A fifth test written for the removal path was DELETED rather than kept: the
  sabotage run showed it passing with the guard removed, because a later check
  already made removal safe. It was evidence of nothing.

  The pass also confirmed, first-hand, the previously-delegated finding that an
  unwritable quota state file silently zeroes accounting across a restart —
  the in-process view hides it until the server restarts. NOT fixed; recorded
  above.
- `tests/plugin-scripts.test.ts` had an early `return` on an unbuilt checkout,
  reporting a pass for assertions that never ran — the exact rule stated
  verbatim in `tests/client-register.test.ts` and followed everywhere else.
  Now `it.skipIf`. FIXED (BUILDER). This is the second instance of this
  pattern found in this cycle.
- `configure --yes` writes `config.yaml` with default permissions
  (`fs.writeFile` with no mode), and `apiKeyForYaml` deliberately preserves a
  LITERAL api_key when writing to disk — so on POSIX a config containing a real
  key can land 0644, in a module family that is otherwise careful (leaderboard
  0600, state dir 0700, client-register carries the original mode). Suspected
  from reading by the fourteenth pass, NOT reproduced: Windows only on this
  machine. NOT fixed.
- Job retention could in principle prune a slot-queued job: `pruneStaleJobs`
  exempts `running`/`queued` only on a fresh heartbeat, and nothing heartbeats
  a slot-queued job by design. Requires a job to sit queued past the retention
  window — 7 days by default. From reading only. NOT fixed.
- An acceptance pass dispatched to the machine's REAL installed harnesses while
  probing fanout, because CLI config entries are additive and its throwaway
  config did not use `disabled:`. Subscription-backed routes only, `read_only`,
  no metered route, and it disclosed the mistake itself. Recorded because the
  brief warned about exactly this and it still happened — the additive default
  is a foot-gun for anyone testing, not just for that pass.
- FIFTEENTH PASS — the full gate: walked every step of `ux-walkthrough.md`
  including Flow 6 (added the previous day, never walked), worked the
  adversarial checklist, and re-attacked the last three days of fixes. It
  confirmed, at the artifact layer, the added-file apply refusal, the
  in-project workspaces root, the safety flag-gap ordering, partial rate-limit
  headers, the quarantined quota file, the legacy blob migration, and the
  unusable-body reporting — all as claimed, in both directions.

  Two defects found, both FIXED (BUILDER), both sabotage-checked. `connect`
  guarded the JSON ROOT and not the `mcpServers` map inside it, so
  `{"mcpServers":"oops"}` still had its string rekeyed into
  `{"0":"o","1":"o",…}` with success reported — the previous day's fix, right
  about the case in front of it and wrong one level down, caught by the very
  next pass. The check now runs at every level that spreads. And the breaker's
  healthy-is-a-contradiction rule never sees a record carrying a deadline, so
  a nonsense one read as a normal route; deadlines are now checked against what
  `snapshot()` can actually produce.

  It also found this record contradicting itself on the all-blocked fanout
  (one bullet "NOT fixed", a later one recording the fix) and its suite figure
  three rounds stale. Both corrected above.

  Suite as of this round: 71 files, 1027 passed, 5 skipped.
- Twenty-five breaker test fixtures used `1_900_000_000_000` — the year 2030 —
  as a "far future" deadline. `snapshot()` emits `Date.now() + remaining`
  capped at `MAX_COOLDOWN_SEC`, so that is a value the product can never write,
  and the new validator correctly rejected every one of them. Replaced with a
  deadline the product could produce. Same rule this suite already states for
  `workspaceRunId`: a fixture is only evidence if it is the thing. FIXED
  (BUILDER).
- `%TEMP%\harness-dispatch\workspaces` accumulates workspace directories the
  test suite creates — the fifteenth pass counted roughly 400 — and nothing
  prunes them, because reclamation only runs during a dispatch against that
  project root. Disk-space housekeeping rather than a correctness defect.
  NOT fixed.
- A gateway that ignores `stream: true` and returns a complete JSON completion
  has its usable answer quoted in the failure message and discarded. This is a
  consequence of the deliberate "the dispatcher no longer classifies" design,
  recorded by the fifteenth pass as a limit rather than a false claim. NOT
  fixed.
- `route-policy.ts` reports "billing source is unknown" for a route `status`
  prints as `billing=metered_api`; `doctor` files a dead client `--config` path
  under "launches from a path that does not exist". Both reach the right
  conclusion with the wrong wording. `parseReset`/`RESET_EPOCH_KEYS` exist but
  `quota.ts` never sets `resetAt` from headers. All from the fifteenth pass.
  NOT fixed.
- SIXTEENTH PASS — aimed at claims that had only ever been INHERITED: recorded
  as verified once, then taken on that pass's word by every pass since. Five
  fixed (BUILDER), each sabotage-checked: an endpoint credential in `base_url`
  reaching the terminal and `dispatches.jsonl` through the exception text the
  redaction wrapped rather than the URL it appended; a second `git_worktree`
  apply reporting a conflict with the user's own apply (the "already applied"
  answer lived on a path a worktree patch never reaches) plus a refusal message
  asserting the opposite of the policy it was refusing; the in-project
  workspaces root breaking apply again from a SUBDIRECTORY, because
  `git status` prints repo-root-relative paths; a `git_worktree` dispatch
  handing back raw git internals for the three ordinary ways it cannot start;
  and the SSE surface emitting an `error` frame for a route failure a fallback
  then recovered.

  Suite as of this round: 71 files, 1037 passed, 5 skipped.
- The libuv assertion and exit 127 on a routed CLI fallback is CONFIRMED STILL
  PRESENT at `f0e5547`, reproduced 3/3 by the sixteenth pass rather than
  inherited: the process aborts AFTER printing the correct answer, and a shell
  reads 127 as "command not found". `--no-fallback` and a refused connection
  both exit 1 cleanly, so the trigger needs a primary that actually answered.
  This is now the oldest confirmed open defect here. NOT fixed.
- A TOP-LEVEL near-miss key on the MCP surface — `safteyProfile: "read_only"` —
  is accepted in silence and the dispatch then runs at the `workspace_edit`
  default. The sixteenth pass measured it writing a file. Asking for read-only
  by way of a typo gets you write access, which is a safety consequence rather
  than an ergonomic one. The nested form (`hints.safteyProfile`) IS rejected, claims-check-ignore
  and an earlier comment claiming "the consequence is the same on both
  surfaces" was over-broad; that is corrected in `tools.ts`. Closing the
  top-level case means taking over `CallToolRequestSchema` below the MCP SDK's
  own routing — every tool's dispatch path — which is not a change to make
  alongside a batch of one-line fixes. FIXED (BUILDER) on 2026-08-31, on its
  own rather than folded into a batch: `mcp/near-miss-guard.ts` wraps the
  CallTool handler the SDK installs and inspects the raw arguments before
  delegating, so routing, validation and the `extra` plumbing are untouched.
  Sabotage-checked twice — once with the check disabled, once with the guard
  left uninstalled in the production builder, since a test that builds its own
  server would stay green while production shipped without it. The parity
  suite's "deliberate asymmetry" block is deleted and both rows moved into
  REJECTED, which is what that block's own comment asked for. Was the most
  consequential
  item on this list.
- OPEN-ITEM SWEEP (2026-08-31), at Felix's request rather than from a pass.
  Every remaining item on this list that was a defect rather than a decision,
  each sabotage-checked:

  The libuv assertion / exit 127 is FIXED. It was the oldest confirmed defect
  here — three passes reproduced it 3/3 and every one of them recorded it as
  pre-existing and unfixed. Cause: `process.exit(code)` tearing the loop down
  while two HTTP connections from the fallback were still closing. Verified at
  the built binary with fresh state on every run, both directions: 127 with the
  assertion 3/3 sabotaged, 0 with none 3/3 fixed. The first sabotage attempt
  was INVALID and is recorded as such — the breaker had tripped on the primary
  by then, so no fallback occurred and the precondition was gone.

  `endpointUrl` appended `/v1` to any base_url not ending in it, which mangles
  every other path. This project's own config.default.yaml documents Google's
  `.../v1beta/openai`, which became `/v1beta/openai/v1/chat/completions`. Fixed
  to use a supplied path as written; a bare origin still gets `/v1`.

  `configure` writes 0600 (POSIX only, unreproduced on Windows, suspected by
  the fourteenth pass from reading). Usage counters that cannot reach disk are
  reported under `status`'s State problems rather than silently zeroing at the
  next restart. `doctor` gained a saved-state check — it read configWarnings
  directly and stayed silent about unreadable state, which two passes recorded.
  Two messages corrected: the unknown-billing refusal now says whether the KIND
  or the CONFIDENCE is unknown, and `doctor` says a client entry "references" a
  missing path rather than "launches from" it, since that path is routinely the
  `--config` argument.

  Not fixed and not a defect: `parseReset`/`RESET_EPOCH_KEYS` exist but
  `quota.ts` never sets `resetAt` from headers. That is unused capability, not
  a wrong answer, and removing it is a judgement call rather than a correction.

- SEVENTEENTH PASS - the near-miss guard, attacked on its own. The wrapper
  itself came back clean in both directions: all six tools still route,
  validate and return unchanged error shapes; output-schema validation, tool
  enable/disable and task support still run behind it; the guard is present on
  every MCP-over-HTTP session, not just the first; _meta and 39 OpenAI
  chat-completion field names produce zero false positives; a double install is
  harmless.

  Four findings, all FIXED (BUILDER), each sabotage-checked. The guard fired on
  every tool while its message was written for dispatch, so on the five tools
  that take no hints it promised a safety consequence that could not happen and
  advised a spelling that is silently ignored there. On dispatch itself,
  correcting the spelling landed on a z.never() trap - a SECOND rejection -
  which is verbatim the lesson tool-schemas.ts already records from its own
  traps, re-learned there and not applied here. The message now names where the
  key belongs, per key and per surface: the HTTP surface reads all seven hint
  names from the top level, MCP honours only two there. installNearMissGuard
  after registerTools was silently inert and now throws.

  The fourth is the one worth repeating: the pass changed the wrapper to pass
  `undefined` instead of the SDK's `extra` argument - severing progress
  notifications for every MCP tool call - and the FULL SUITE passed, 1041
  tests, zero failures. The single thing this wrapper could most plausibly
  break had nothing holding it, while the CHANGELOG asserted "progress
  reporting untouched". There is a test now, and it fails under that exact
  sabotage.

  Suite as of this round: 71 files, 1045 passed, 5 skipped.
- The test suite DISPATCHED TO REAL SUBSCRIPTION HARNESSES on every run. CLI
  config entries are additive, so tests/surface-parity.test.ts writing empty
  clis/endpoints lists did not isolate it: claude_code_cli, codex_cli,
  cursor_cli and antigravity_cli were all configured and reachable, and its
  boundary test reached the real Claude Code CLI - measured by the seventeenth
  pass at 6.4s and 47k input tokens, on every npm test, every npm run check and
  every CI run. The test carried the comment "This call fails later for an
  unrelated reason (no configured routes)", which was false and which nobody
  had checked. The config now carries an explicit disabled: block naming every
  auto-detected route. Verified by direct measurement rather than by reading:
  the real dispatch log stood at 425 lines before a full npm run check and 425
  after. FIXED (BUILDER).
- ux-walkthrough.md described the MCP near-miss hole as CURRENT for a day after
  it was closed - a shipped document telling a reader a safety gap is live, in
  an artifact the acceptance gate itself reads. check-claims.mjs passes on it,
  so nothing catches this class. FIXED (BUILDER).
- The near-miss hint warning was MCP-blind, and is not any more — see the entry above. This bullet described the same defect from the thirteenth pass; both are kept so the history reads in order, and this one is marked so the file does not carry an open item that is closed two entries up. FIXED.
- A cancelled job still reports `nextPollSeconds: 300` and "check again until
  status is completed or failed". `completed: true` is correct, so an agent
  following the documented field is fine. Cosmetic. NOT fixed.
- `endpointUrl` inserts `/v1` into any base_url not ending in it, so an
  `anthropic_messages` route documented as `POST {base_url}/messages` actually
  posts to `/v1/messages`. Third-party hosts on a non-`/v1` path are
  unconfigurable. Verified in passing by the thirteenth pass. NOT fixed.
- Top-level `workspace_policy:` and `policy:` are allow-listed, read nowhere,
  and produce no warning — an isolation-control name that does nothing, which
  `validation.ts`'s own comment calls the failure it exists to prevent.
  NOT fixed.
- No numeric validation on route fields. `tier: -5, weight: -100,
  cli_capability: -3` pass straight through and score 299.8 against a normal
  route's 0.88, winning every routing decision. NOT fixed.
- If `quota_state.json` cannot be written, quota accounting silently stops:
  `usage` reports `callCount: 0` after real dispatches, with no error and no
  change of `source`, while `doctor` reports the state directory writable.
  `usage` is the surface both `CLAUDE.md` and `OPERATIONS.md` tell an
  orchestrator to consult before delegating. Measured by a delegate, not
  reproduced by the acceptor. NOT fixed.
- A streamed request that falls back successfully still emits an SSE `error`
  frame for the failed attempt. A client treating `error` as terminal discards a
  good answer. Predates this cycle.
- Streaming creates no job record, so an interrupted stream cannot be recovered
  and there is no `jobId` to poll. Documented in `README.md` and
  `OPERATIONS.md`; not fixed, because routing streaming through the job system
  is a redesign rather than a fix.
- Telemetry spans are never flushed — `shutdownObservability()` is exported and
  called from nowhere — and OpenTelemetry's default process detector would carry
  argv, which for `dispatch "<prompt>"` contains the prompt. Off by default,
  behind an explicit opt-in, and documented as a known defect in
  `OPERATIONS.md`. The two must be fixed together: fixing the flush alone ships
  the leak.
- The test suite fails intermittently under load with resource-shaped errors
  (`spawn UNKNOWN`, a failed `git commit`, one "the paging file is too small"
  from a concurrent dispatch). Parallel process-spawning exhausting the machine
  rather than a logic race; three consecutive runs on an idle machine were
  green. `npm run check` is not a reliable gate on a busy machine.
- `PRODUCT.md` declares no provenance, so the acceptance gate's
  `A-intent-anchored` check is `not_evaluated` and SHIP is unreachable. This
  verdict covers consistency and build quality, not whether this is the right
  product.

## Verified

**Live dispatch (seventh pass).** `node dist/bin.js dispatch --service claude_code_cli --safety
read_only --no-fallback "Reply with exactly one word: pong"` → `pong`,
`success: true`, 6,779 ms, 48,007 input / 4 output tokens. Run against the build
in the working tree. No metered route, no `--allow-paid`, no `doctor --live`.

The pass also disclosed two unplanned live calls: a second `claude_code_cli` run
through the MCP `dispatch`→`job_status` path, and one accidental `codex_cli`
dispatch at `workspace_edit` caused by assuming `--config` replaces the shipped
routes. It does not — `clis:` entries are additive, and the only lever that
isolates you from your real routes is `disabled: [...]`. That is documented
behaviour and worth knowing before writing a throwaway config.

**The four fixes no earlier pass had seen**, all measured against `dist/`:

- HTTP streaming stops rather than falling back once answer text has been sent.
  A primary that streamed two fragments and then destroyed the socket produced
  both deltas, an error frame, and **zero** fallback route hits. A primary that
  failed *before* sending anything still fell back and delivered the fallback's
  full answer. Both directions correct.
- `connect --yes` refuses to overwrite a hand-edited entry, exit 1, file
  byte-identical; `--force` overrides while preserving the entry's `env` and
  unrelated servers, and takes a backup. Backup pruning holds at three.
- `git_worktree` unregisters and removes a worktree when an attempt failed and
  changed nothing, keeps one that changed files with a `cleanupHint`, and leaves
  `git worktree list` clean after a scored dispatch whose fallback arm also
  failed — the case that previously left two entries.
- The CHANGELOG's false claim that a server start drains the slot queue is gone.

**Suite.** `npm run check` clean. The count moves with every round of fixes
recorded below, so it is stated as of the last one rather than kept in two
places: see the final entry under "Open at ship time" for the current figure.
An earlier version of this line froze at "960 passed, 4 skipped" and was three
rounds stale by the time a pass checked it.

**Walkthrough.** Every step of `ux-walkthrough.md` happened as written: `status`
without a config file, `doctor`'s eleven named checks, exactly six MCP tools and
two resources, async dispatch returning `completed: false` plus a `jobId`,
`partialOutput` tailing a running job, `usage` counting the detached runner's
work, `job_status` with no id returning `{"jobs": []}`, the no-routes empty
state, and `cancel_job` moving a running job to `cancelled` within a second
while warning that changed files are not reverted.

**Changelog audit.** Every reachable claim in `[Unreleased]` was checked against
the artifact. Three problems were found and fixed after the pass: `GET /health`
and `OPERATIONS.md` were filed under `### Fixed` when both are additions, and
"2.7x the ceiling" was a composed figure (the numbers given yield 2.65x) in both
`CHANGELOG.md` and `config.default.yaml`. The machine-history numbers — 840
project directories, a month of dispatch logs, the 13,554 and 21,670-character
prompts — could not be re-verified from a checkout and are carried on the
record of the passes that measured them.

**Also confirmed:** `routing.candidates` now reaches `dispatches.jsonl` on
scored dispatches, which closes the one item 0.7.9 shipped open.

**Live dispatch (eighth pass).** The same one-liner on `claude_code_cli` →
`pong`, `success: true`, 10,279 ms, 48,804 input / 4 output tokens, logged with
`safetyProfile: read_only` and `reason: explicit`. No metered route, no
`--allow-paid`, no fanout.

**Quota accounting is exact.** Across 4 successes, 2 failures, 1 cancellation, 1
retry and a two-arm fanout, `usage` matched on-disk job truth: cancelled jobs
are not counted, each fanout arm counts once, and a retry counts as its own
call. Two concurrent `shared_locked` dispatches on one working directory
serialised strictly with zero overlap, and concurrent `cancel_job` is
idempotent.

**Config error handling.** Malformed YAML, a missing `--config`, a route with no
command, an unknown harness and missing endpoint fields all fail loudly with
actionable messages and exit 1. CLI exit codes are correct: 1 on an unknown
service and on a failed dispatch.

## Not checked

The interactive TTY consent prompt for `connect` (no pty available). POSIX file
modes — this pass ran on Windows, Node 24.14.1 only; the `engines` floor of
22.22.2 is untested here. The npm publish and OIDC flow. A dependency
vulnerability audit. `doctor --live`, declined on subscription-cost grounds per
`acceptance/README.md`. `router.ts`'s scoring changes and the `mcp/tools.ts`,
`config.ts` and `status.ts` diffs, which the code audit did not reach. Items
excluded as covered by earlier passes — workspace diff/apply/discard, the npm
tarball, long-prompt dispatch, MCP/CLI boundary validation, HTTP auth and
limits, concurrency and queueing, config hot-reload, breaker recovery, fanout
validation, MCP resources, `contextJobs`, escalation config, the leaderboard,
`auth rotate`, endpoint redaction, the plugin install script, the fixture guard
— were taken on those passes' word rather than re-run.

## Before the tag

`package.json` is still `0.7.9`, `[Unreleased]` has no version heading or date,
and `CHANGELOG.md` has no `[0.8.0]:` compare link. The 0.7.9 pass had to call
out the same three edits; they are outstanding again.

## Open-item triage (2026-08-31, second sweep)

At Felix's request rather than from a pass: every item on this list still
reading "NOT fixed" was re-checked against `main` at 8fc2845 before any of it
was treated as work. Five had already been closed by other changes and the
record had not caught up. Verified closed, each by reading the code that now
handles it:

- The libuv assertion / exit 127 on a routed fallback — closed by the first
  open-item sweep above.
- `configure --yes` writing `config.yaml` at default permissions — `bin.ts`
  writes `mode: 0o600`, with a comment recording that the mode applies on
  create only.
- `endpointUrl` inserting `/v1` into any base_url — now decides on the parsed
  URL's pathname, so a host on a non-`/v1` path is configurable.
- Quota accounting stopping silently when `quota_state.json` cannot be written
  — `QuotaCache` records `persistError` and `buildStatus` reports it.
- A cancelled job answering `completed: false` forever — `jobCompleted` treats
  cancelled as terminal.
- `parseReset` / `RESET_EPOCH_KEYS` described as dead — they are read by
  `rate-limit-headers.ts` and reach `resetAt`; not a defect.

Two were fixed here, each sabotage-checked separately and in both directions:

- **Numeric route fields had no range check.** The type check added earlier
  catches `tier: metered`; it does not catch `tier: -5`, because that value IS
  readable — which is precisely what makes it dangerous. Routing multiplies
  `weight` and `cli_capability` and sorts `tier` ascending, so the measured
  `tier: -5, weight: -100, cli_capability: -3` scored 299.8 against 0.88 and
  sat in a tier ahead of every real one. Out-of-range values are now warned
  and dropped, so the default applies — identical treatment to an unreadable
  value, which is what both warnings already promised.

  Sabotaged twice on purpose, because the fix has two independent halves.
  Removing only the drop failed the three behaviour tests and left the warning
  test passing; suppressing only the warning failed the warning test alone. A
  single sabotage would have proven neither half.

  No upper bound was added. `cli_capability: 1.1` ships in this repo's own
  `config.default.yaml`, so a cap would reject a documented value; a test pins
  that 1.1 still loads and warns about nothing.

- **Top-level `policy:` and `workspace_policy:` were allow-listed and read
  nowhere.** Same shape as `default_safety_profile`, and they join it in
  `ACCEPTED_BUT_UNIMPLEMENTED`. Re-verified read-nowhere first: the `raw?.policy`
  in `jobs.ts` reads a job manifest, not this config file. A third test pins
  that the per-route `workspace_policy` still applies, so the warning cannot be
  read as "this key is dead everywhere".

Three remain open, all judged rather than forgotten:

- `pruneStaleJobs` can in principle delete a slot-queued job: the
  running/queued exemption needs a fresh heartbeat and nothing heartbeats a
  job waiting for a slot. It needs the pool saturated for the full retention
  window — 7 days by default — and is still from reading, never reproduced.
  Left open rather than fixed: the obvious fix (exempt `queued` regardless of
  heartbeat) makes a queued job whose owner died un-prunable forever, which is
  a worse failure than the one being prevented. NOT fixed.
- Roughly 400 workspace directories accumulate under
  `%TEMP%\harness-dispatch\workspaces` from test runs, because reclamation
  only runs during a dispatch against that project root. Disk housekeeping,
  not correctness. NOT fixed.
- Streaming creates no job record, so an interrupted stream has no `jobId` to
  poll. Documented in `README.md` and `OPERATIONS.md`; routing streaming
  through the job system is a redesign, not a fix. NOT fixed.

Suite after both fixes: 1082 passed, 5 skipped, claims check clean. The real
dispatch log stood at 425 lines before this session's work (recorded by the
previous sweep) and reads 425 after the full run, so nothing here reached a
real harness.

## Nineteenth pass (2026-08-31) — independent acceptor, verdict CONDITIONAL

Run against `main` at 0e04a9e, immediately after the open-item triage above.
The acceptor did not write any of this code, was assigned the review as its
own task, and saw no plan or self-assessment — so the cap was honestly lifted
and this is the second uncapped verdict on record. The mechanical gate
returned SHIP on all eight checks; the acceptor OVERRODE it to CONDITIONAL,
which is the gate working as designed rather than against it.

Eight findings, all reproduced. The two that matter most were both about
work committed hours earlier, and both are the same failure the builder keeps
making: the fix is right about the case in front of it and wrong one case
over.

- **The range check did not cover non-finite values, and its warning was
  false.** `readsAsNumber` used `Number.isFinite` on the number branch but
  `!Number.isNaN(Number(v))` on the string branch, and `Number("1e999")` is
  `Infinity` — so a YAML `weight: 1e999` (a string to YAML) passed the type
  check, then passed the range check too, because `Infinity` is below no
  minimum. It loaded as `weight = Infinity` with NO warning of any kind.
  Separately, `.inf` and `.nan` ARE `typeof "number"`, so they were caught by
  the type branch — which warned without neutralising, because for an
  ordinary unreadable value `num()` returns the default anyway and the
  warning was true by accident. The measured result was a route at
  `tier=-Infinity, weight=Infinity` — ahead of every tier and above every
  score — under a warning reading "IGNORED, and the built-in default applies
  instead".

  That last part is the worst of it. The previous state was a silent defect;
  the state shipped hours earlier was a defect with a message asserting it
  had been handled. Fixed by making both branches finite-only and having the
  type branch delete too.

- **`clis: []` did not isolate a config, while the comment explaining the
  authoritative-config change named `clis: []` as its motivating failure — in
  the past tense.** `definesRoutes` required a NON-EMPTY array. Measured with
  every harness pretended present: `clis: []` returned all four real CLIs.
  The commit that claimed to fix this did not cover its own worked example,
  and this project's test isolation still leaned on `disabled:` naming every
  route — the mechanism that same commit argues decays. Fixed by keying on
  presence of the key rather than its length.

  Recorded plainly because it is a false factual claim written into merged
  code and a commit message, not merely an incomplete fix.

Four more, all user-facing rather than internal:

- The README's documented "adding a harness" recipe DELETED every detected
  route, silently. Following the docs cost you your harnesses. The no-routes
  direction warned loudly; the route-loss direction said nothing. The recipe
  now carries `detect: true` and a table of the four intents.
- `configure` generated a config that fails its own `doctor` (exit 1): it
  carried `disabled:` forward next to the `clis:` it writes, where the
  control does nothing.
- `config.default.yaml` — the file users are told to copy — documented the
  pre-0.9.0 rule about `disabled:` inside itself.
- A `clis:` written as a mapping rather than a list was dropped in silence,
  falling back to auto-detection: the user naming their own routes got every
  installed paid harness instead.

Two low-severity: legacy `services:` configs never reached the top-level key
check, and the range warning explained routing for `timeout_ms` and the token
caps, which routing does not score.

All eight fixed. Six sabotage checks, each run separately and each failing
only its own tests — the encoding of one sabotage run crashed midway and left
the first sabotage applied on disk; it was restored and the whole set re-run,
recorded here because a sabotage left half-applied is exactly how a green
suite comes to mean nothing.

The acceptor also verified as TRUE: execute-to-endpoint refusal at the
running binary, ordinary out-of-range handling, `cli_capability: 1.1` still
loading, all three `detect:` cases, and a sample of the triage above
(`bin.ts` 0o600, quota `persistError`, cancelled-as-terminal).

Not covered, and stated as not covered: workspace diff/apply/discard, the
HTTP surface and its auth, fanout job-backing and orphaned partial progress
(read, not run), streaming, quota and breaker behaviour, plugin scripts, and
everything POSIX-only. No route was dispatched to a real harness, so nothing
about real-harness behaviour is verified by this pass.

Suite after the fixes: 1094 passed, 5 skipped, claims clean. Real dispatch
log 425 before and 425 after, both measured.

## Twentieth pass (2026-09-01) — independent acceptor, verdict CONDITIONAL

Run against `fix/acceptance-pass-19` at 3bd2c3f (unmerged). Cap lifted
honestly. The acceptor was steered two ways: re-attack the nineteenth pass's
eight fixes, and then spend most of its effort on the areas that pass listed
as unexamined — because nineteen passes had concentrated on config and
routing, and a defect cannot be found where nobody looks.

Both instructions paid, in opposite directions.

**Part (a): the eight fixes held.** Every one verified by loading real YAML
through the built `dist/config.js`, including the README snippet and the
shipped `config.default.yaml` loaded verbatim. One low finding: the
NOT-A-NUMBER branch still emits the routing consequence for all six keys,
while the range branch was fixed to vary it — the same defect class, one
branch over, in the same function, found by the very next pass. Cosmetic.

**Part (b) is where the release-blocking defects were, and both had been
sitting unexamined the whole time.**

- **A `git_worktree` apply reported files applied that never landed.** The
  patch is built with `git add -A -N`, which obeys `.gitignore`;
  `changedFiles` comes from a filesystem fingerprint, which does not. So an
  agent that wrote a gitignored file got `applied: true` naming it, with the
  file in neither the patch nor the project. It compounds: the already-applied
  guard needs every recorded change present, so it never fires, and the next
  apply refuses with "changed since dispatch" — blaming the caller for the
  first apply's own writes, which is the misleading refusal that
  `workspace-resolve.ts`'s own comment says was fixed. `discard` still refuses
  to destroy the content, which is the only reason this is recoverable at all;
  but a caller told "applied" has every reason to force past it.

  FIXED: force-add exactly the paths already recorded as changed, never a
  blanket `-f -A` (which would sweep whole ignored trees in). Sabotage:
  removing the force-add failed all three new tests and nothing else.

- **An orphaned job's progress never reached the caller.** This one is worth
  recording as a lesson rather than an entry. `jobs.ts` reads
  `output/stdout.partial.log` for an orphaned job under a thirteen-line
  comment citing PRODUCT.md's defining criterion. `mcp/tools.ts` counts
  orphaned as terminal — correctly — and attached `partialOutput` only on the
  NOT-terminal branch, so `job_status` answered `output: ""`. The acceptor
  confirmed the log on disk, confirmed `getAsyncJob` returning it, then asked
  through the tool and got an empty string.

  Commit 04d76d8 is titled "an orphaned job hands back its progress". It was
  verified at the function it edited and never at the tool a caller invokes,
  and it was merged with that claim. `CLAUDE.md` states the rule it broke in
  as many words: verify at the layer the user touches, not the layer you
  edited. The same path was dropping output for cancelled jobs and for the
  crash path.

  FIXED at `jobRouteResponse`, with the new tests written against
  `invokeTool("job_status", ...)` rather than against the reader — the gap
  between those two is the entire defect. The response now also says the
  output is PARTIAL, because salvage mistaken for a finished answer is worse
  than the empty string it replaces. Sabotage: reverting the output failed two
  tests, suppressing the marker failed one, and the no-progress guard passed
  under both, which is what stops the fix over-reaching into a lie about an
  empty log.

Also found and NOT fixed here: `apply`'s dirty-tree refusal exists only inside
a git repo while the tool describes it unconditionally (medium, reproduced;
the per-file hash guard still applies outside a repo, and `copy` is the
documented non-repo policy), plus around a dozen read-only suspicions —
`withFileLock` running its critical section unlocked after a 2s timeout while
`saveLocalCountsSync` clears the pending delta as though it wrote,
process-local breaker state not honouring a sibling's trip, `contextJobs`
having no working-directory scoping, an orphaned job being uncancellable but
still claimable, `discard` skipping `git worktree prune` when the root is
already gone, and `trip()` never incrementing the failure count so `usage`
shows `tripped: true, failures: 0`. Each needs its own reproduction before it
is treated as real.

**A correction to this file's own method.** Every pass since the fixture-guard
work has cited "real dispatch log 425 before and after" as evidence that
nothing reached a paid harness. The acceptor established that
`tests/setup-env.ts` redirects `HARNESS_DISPATCH_LOG_DIR` to a temp directory,
so the real log would not move even if a test DID dispatch. The count is sound
evidence about probes run outside vitest, which is how the acceptor used it —
it is not evidence about the suite, which is how this file has been using it.
The acceptor declined to run the full suite for that reason. The question of
whether any test reaches a real harness is therefore OPEN, and the number
previously offered as its answer never addressed it.

**Areas examined and reported clean**, which is the other half of what this
pass bought: HTTP bearer auth against a live server (five bypass shapes, all
401; `/health` the only unauthenticated route), fanout arms genuinely
job-backed with their job directories on disk, streaming frames, HTTP boundary
handling, the entire `copy` workspace lifecycle including refusals and forced
discard, `HARNESS_DISPATCH_WORKSPACES_DIR` inside the project, retention
arithmetic, breaker persistence and cooldown clamping, and `doctor`'s empty
state. None of it had been exercised by any previous pass.

Not reached: the full suite (deliberately), quota and breaker behaviour under
real concurrency, `leaderboard.ts`, `near-miss.ts`, `client-register.ts`,
`observability/`, `status.ts` rendering, `connect`, `configure`'s interactive
path, MCP-over-HTTP session lifecycle, and `contextJobs` at runtime.

Suite after both fixes: 1101 passed, 5 skipped, claims clean.

## Suspicion sweep (2026-09-01) — reproducing the twentieth pass's read-only list

At Felix's request, and prompted by a question worth recording: "do we have
too many tests?" The measurement said no — 22.4k test lines against 21.5k
source lines, roughly 1:1 — but it also said the suite is distributed around
what has BROKEN before rather than what the product promises, which is why two
passes running found release-blockers in code that had been green for weeks.
The agreed response was not to prune or to add coverage speculatively, but to
reproduce the twentieth pass's read-only suspicions and write tests only for
the ones that turned out to be real.

Reproduced and FIXED, each sabotage-checked separately:

- **Quota counters lost under a contended lock.** `withFileLock` runs its
  critical section unlocked after a 2s timeout. That is deliberate and its
  docblock argues it well — for BreakerStore, which has nothing to fall back
  on. The argument does not transfer to QuotaCache, which keeps a pending
  delta and clears it only on success: the unserialised write lands, the delta
  is cleared as though serialised, and the real lock holder then overwrites
  the file from a baseline taken before that write. Measured against the built
  artifact: holder at 0, five calls written unlocked, holder's +3 landing last
  — final count 3, five calls gone, no error. Now `requireLock: true` for that
  caller only; the delta stays pending and lands on the next result (verified:
  final count 5). A lock miss deliberately does NOT set `persistError` —
  delayed is not undurable, and reporting it would be the same class of lie in
  the other direction.

  A second measurement worth keeping: `recordResult` persists on EVERY result,
  so while a stale lock sits there each result stalls the full 2s — five
  results measured at 10.03s total, on the dispatch return path. The stale
  steal at 10s bounds it. Not separately fixed; the deferral above is what
  makes the stall harmless rather than lossy.

- **`trip()` never counted the failure**, so `usage` showed
  `{"tripped":true,"failures":0}` for a route knocked out by a 429. Fixed with
  `lastFailureAt` set too — not scope creep: a counter with no failure time
  never decays, so incrementing without it would have been a worse bug than
  the one being fixed. An existing snapshot test asserted `failures: 0` here
  incidentally (its subject is wall-clock conversion) and was updated with a
  note saying why.

- **`discardWorkspace` left a worktree registered** when the workspace root
  was already gone: the early return skipped the removal-through-git block, so
  retention pruning or a manual delete left `.git/worktrees/<name>` in the
  user's repo forever — the outcome that function's own docblock says it
  exists to prevent. Reproduced with `git worktree list` showing the path as
  `prunable` after `discarded: true`. Prune, not remove: the directory is
  already gone.

Also added, because nothing pinned it and it is the layer-gap class that has
now bitten three times: a test that `usage`'s STATUS PAYLOAD carries the
counter-persistence warning, not merely that the cache records it. The cache
side was already pinned; the surface was not.

Probed and NOT reproduced, recorded so the next pass does not re-litigate
them from the same reading: `contextJobs` cross-project scoping — the leak is
plausible from the code, but the fixture built for it was not read back by
`buildContextPreamble` ("this job is unknown, still running, or was pruned"),
so nothing was demonstrated either way. It stays an open suspicion, not a
finding.

Still untouched from that pass's list, none reproduced: process-local breaker
state not honouring a sibling's trip, an orphaned job being uncancellable but
still claimable, `MAX_PATCH_BYTES` bounding only the worktree path, `copy` not
preserving directory modes, `.supervisors/` growing without bound, and
`apply`'s dirty-tree refusal existing only inside a git repo (that last one
was REPRODUCED by the twentieth pass and remains unfixed by choice — the
per-file hash guard still applies outside a repo, and `copy` is the documented
non-repo policy).

Suite: 1109 passed, 5 skipped, claims clean.

## Closing the open list (2026-09-01)

Every remaining item from the nineteenth and twentieth passes, at Felix's
request. Seven fixed, each reproduced first and sabotage-checked separately.
Two of the eight sabotages did not discriminate on the first attempt and are
recorded below, because that is the part worth keeping.

- **cancelJob refused every orphaned job.** The fix was initially too broad
  and an EXISTING test caught it. There are two kinds of orphaned job:
  `drainSlotQueue` WRITES the status for a slot-queued job abandoned at
  shutdown (terminal by its own error text), while `withOrphanCheck` DERIVES
  it from a stale heartbeat with the file still reading `queued` (not
  terminal — a supervisor reclaims it once the dead owner's claim ages out).
  Cancelling both was wrong; the raw file now decides. `jobs/types.ts` claimed
  "orphaned is never written to disk", which is false and is what produced the
  over-broad first attempt — corrected in place.

- **`copy` patches were unbounded in total.** The first test for this passed
  with the bound REMOVED, which is how a wrong test gets caught: a single huge
  file trips the per-file execFile maxBuffer and never reaches the
  accumulation check, so the test was proving a different fix. Rewritten with
  six 500KB files — each far under the limit, together over it — and it now
  discriminates. The per-file cap got its own test too, after ITS sabotage
  also came back green.

- **`.supervisors/` grew forever.** A killed supervisor cannot delete its
  heartbeat, so the file stopped being counted but stayed, and the liveness
  check reads every file on every drain. The sweep is deliberately limited to
  `<id>.txt`: the directory also holds `spawn-<id>.log`, which exists to
  explain a supervisor that DIED — the same one leaving the stale heartbeat.
  A sweep over every file would have deleted the diagnostic for the failure it
  was cleaning up after. Caught while writing the code, not by a test.

- **Isolated workspaces were world-readable on POSIX.** 0700 now, matching the
  state directory and every job directory; this module was the only one in the
  family with no mode at all. Applied at the roots, not to every copied
  directory — a 0700 ancestor already blocks traversal. Its test is POSIX-only
  and therefore does NOT run on this machine; the first version of it asserted
  on the run directory, which `copyTree` creates and the fix does not touch,
  so it would have pinned something untrue and failed only in CI. Retargeted
  at the directory the fix actually creates.

- **`contextJobs` had no working-directory scoping** — reproduced: a job from
  one project chained into a dispatch for another, prompt and output carried
  verbatim. DISCLOSED rather than blocked: cross-project chaining is a
  legitimate thing to want and the caller passes the id explicitly, so this is
  a missing guardrail rather than an injection route. What was actually wrong
  is that neither side could see it.

- **`workspace apply`'s description promised a refusal it cannot always make.**
  The uncommitted-changes check needs a git repository; outside one only the
  per-file hash check applies. Fixed the description rather than the
  behaviour: there is no cheap way to ask "is this dirty" without a baseline,
  and `copy` is the documented policy for non-repos.

- **The not-a-number branch still claimed routing reads `timeout_ms`.** The
  range branch beside it had been fixed for exactly this a day earlier.

Also verified in passing and NOT changed: `refreshBreakersFromStore` is called
only from the reporting path, so a sibling process's trip is not honoured
until this one finishes a dispatch of its own. Left alone deliberately — the
store IS consulted on the write path, the staleness window is one dispatch,
and making scoring re-read the file on every candidate would put a disk read
in the routing loop for a case that self-corrects.

Suite: 1120 passed, 6 skipped (the extra skip is the POSIX-only permissions
test), claims clean. Real dispatch log 425 at both ends — and per the
correction recorded above, that is evidence about the probes run outside
vitest, not about the suite.

## Answering the open question: does the suite reach real harnesses? (2026-09-01)

This question had been asked across three passes and answered twice with
evidence that did not address it — "real dispatch log 425 before and after",
when `tests/setup-env.ts` redirects that log to a temp directory and it would
not move even if every test dispatched. Measured properly:

- Shims named `claude`, `codex`, `cursor-agent` and `agy` placed AHEAD of the
  real binaries on PATH, each recording any invocation. Full suite: ZERO
  invocations. The suite also passed identically (1120 passed, 6 skipped),
  which answers the second half — no test depends on a real harness being
  present either.
- Every outbound connection recorded via an `--import` preload wrapping
  `fetch`, `http.request` and `https.request`. One non-localhost request in the
  entire run, to `no-such-host-xyz.invalid`, from a DNS-failure test.
- The six Windows-skipped tests were checked by name: all are POSIX
  file-mode/symlink tests. None dispatch, so the platform gap hides nothing.

Limits, stated rather than implied: measured on Windows only; CI's Linux and
macOS runs were not instrumented. The network recorder logs without blocking,
so a test replacing `globalThis.fetch` bypasses it — which means it is not
making a real call anyway.

**A guard was then added, and the reasoning for choosing it over a check
matters.** The first proposal was a repeatable script or CI job running the
shim setup. Rejected: a second full run someone must remember, which detects
only after the fact and rots. `HARNESS_DISPATCH_CONFIG` makes the failure
impossible instead, in three lines — setup-env pins it at a `detect: false`
sandbox, so an un-stubbed config load gets an empty route table. Cost measured
before deciding: exactly one test broke, `cli.test.ts`'s config-precedence
test, which occupies the rung above the one it tests and now owns the variable
itself.

**The hazard was wider than assumed, and a test of mine failed to catch it.**
The third assertion written for the guard listed the four harness CLI names and
PASSED under sabotage. Investigating why: with the guard removed,
`resolveConfigPath()` on this machine finds the repository's own `config.yaml`,
and a bare load yields `groq_api`, `gemini_api`, `router9_api` and
`local_inference` — the maintainer's real config with real API keys. The
reachable routes were not CLIs at all. That test was DELETED rather than
extended: asserting the route table is EMPTY already covers every kind of
route, including the ones nobody thought to enumerate. Recorded because the
sabotage is the only reason the gap was found, and because "list the dangerous
names" is exactly the shape of guard that keeps being wrong here.

Suite: 1122 passed, 6 skipped, claims clean.

## Twenty-first pass (2026-09-01) — independent acceptor, verdict BLOCK

Run against `main` at fad92e1. **The acceptor declined to lift its
independence cap, and the reason is a mistake in how it was briefed:** the
prompt told it to read this file, which is the builder's own written
self-assessment of the work under review — precisely what the cap rule's
second condition excludes. It said so rather than accepting the instruction.
Future passes should be pointed at the CODE and the CLAIMS, not at this
record; naming the claims in the prompt is enough.

It overrode to BLOCK regardless, on two reproduced defects in commands the
documentation tells people to run. Both are fixed below; the rest of its
findings are recorded and NOT yet addressed.

### The two blockers, both reproduced by the builder before fixing

- **`configure --force` silently deleted `detect: false`.** That key is the
  only thing that isolates a machine from its installed paid CLIs — this
  suite's own `setup-env.ts` depends on it — and it had no field on
  `RouterConfig`, so nothing carried it through a regenerate. Measured:
  `detect: false` + `max_concurrent_runs: 2` regenerated as the latter alone,
  and reloading with all four CLIs present yielded claude_code_cli, codex_cli,
  cursor_cli and antigravity_cli. A bare `detect: false` regenerated as `{}`,
  where the "defines no routes" warning is ALSO suppressed because its trigger
  requires a non-empty document. Fixed by carrying `detect` on the config
  object and emitting it — only when the file stated it, since writing the
  resolved value would stamp `detect: true` into every config that merely
  omitted it. Verified at the built binary in both directions, and by
  reloading the regenerated file with every harness pretended present: no
  routes.

- **`connect --remove` removed nothing and reported success.** The chooser
  filtered plans whose state is `matches` — right when registering, backwards
  when removing, where `matches` IS the entry to remove. Byte-identical md5
  before and after, exit 0. `--clients` bypasses that function entirely, so
  only the bare form broke: the one README documents twice and OPERATIONS.md
  once. Rewritten as what IS actionable rather than what is not — the first
  attempt was a negation and let `absent` (no config file at all) through.
  A second improvement falls out: a hand-edited entry is now actionable and
  refused, so the command exits non-zero instead of reporting success for work
  it did not do.

  **This command had no test at all.** `tests/client-register.test.ts` covers
  the module; nothing covered the CLI driving it. `tests/connect-cli.test.ts`
  now does.

### Recorded and NOT fixed

Reported here so the next round has the list rather than rediscovering it.
Highest first:

- The 0700 workspace fix is PARTLY FALSE, and the CHANGELOG entry for it
  overclaims. `mkdir(recursive, mode)` does not chmod a directory that already
  exists, so every user who used `copy` before that commit keeps a 0755 root
  forever — the upgrade does not fix them, and the entry says it does. Worse,
  the root path is deterministic inside a shared `os.tmpdir()`, so another
  local user can pre-create it 0777 and `markProjectRoot` swallows the error.
  Measured on real Linux by the acceptor, both fresh and pre-created.
- `retryJob` did not get the reclamation guard `cancelJob` was given: a
  derived orphan is retryable while the original stays claimable, producing
  the two-runs-racing outcome retry's own error text says it prevents.
- HTTP: every failed session lookup leaks an `McpServer` permanently, and
  sessions are never expired or capped.
- `leaderboard.ts`'s 8s timeout does not cover reading the response body, so a
  half-dead endpoint wedges every dispatch indefinitely. Off by default, which
  is the only reason it is not a blocker.
- `contextJobs` silently DROPS jobs past the character budget — no header, no
  note — contradicting its own docblock, and truncates newest-last while the
  docblock claims oldest-first.
- The copy path's size refusal names the parent of the project rather than the
  workspace.
- `.supervisors/` still grows: the sweep is `.txt`-only by design, and nothing
  ever deletes `spawn-<id>.log`.
- Cancelling an orphan that HAD started records "Cancelled before it started."
- Roughly fifteen smaller items, several of them messages that describe
  behaviour the code does not have.

### Verified clean by the acceptor

cancelJob's two directions; both patch limits; quota deferral under
contention; `trip()`; test isolation under PATH shims (0 harness invocations,
1122 passing, dispatch log unmoved); `status.ts` redaction on every path;
`configure`'s overwrite guard, 0600 mode and `${VAR}` restoration;
`client-register`'s consent gate, shape guards, backup retention and
atomicity; `near-miss`'s typo distance checked exhaustively against a
reference implementation; `leaderboard`'s clamping, atomic cache write and
enabled-gate; observability spans leaking no prompt text or credentials.

Suite after the two fixes: 1131 passed, 6 skipped, claims clean. Real dispatch
log 425 at both ends of the pass and of the fixes.

## Closing the twenty-first pass's list (2026-09-02)

Seven items fixed, each reproduced before being touched and each
sabotage-checked. Two sabotages were WRONG on the first attempt and are
recorded, because both would have left a fix unproven while looking verified.

- **The 0700 workspace fix was partly false and its CHANGELOG entry
  overclaimed.** `mkdir`'s `mode` applies only to directories it creates.
  Measured in a Linux container: a pre-existing 0755 root is still 0755 after
  `mkdir(recursive, 0o700)`. So the release "fixed" nobody who had used `copy`
  before it. Now chmod'd explicitly, and a root owned by another uid is
  REFUSED — the path is deterministic inside a shared `os.tmpdir()`, which is
  the multi-user threat the guard's own comment names. Verified in a container
  as uid 1000 against a root-owned directory: refused with a message naming
  both uids.

  Its sabotage could not run here: the test is POSIX-only and skips on
  Windows. Done at the BUILT ARTIFACT in the container instead — chmod removed
  gives 755, restored gives 700 — which is stronger than the test-level check
  it replaced.

- **`retryJob` produced exactly what its own error text forbids.** Its guard
  reads the DERIVED status (`orphaned`, so it passes) while `claimNextJob`
  filters on the RAW file (`queued`, so it claims). `cancelJob` was taught to
  tell the two kinds of orphan apart a day earlier and retry was not — the
  same fix stopping one square short, which is now the recurring shape of this
  codebase's defects.

- **`contextJobs` dropped jobs past the budget silently**, contradicting the
  contract stated two functions above it, and its docblock claimed
  oldest-first truncation when the loop drops the last entries.

- **The leaderboard's 8s timeout did not cover the response body.** The first
  test written for this was DECORATIVE — it exercised the shape of the fix
  with its own fetch rather than the code path, so sabotaging `leaderboard.ts`
  could not have failed it. Replaced with one that stubs `fetch` to answer
  headers and hang on the body. The first SABOTAGE was also wrong: removing
  `clearTimeout` entirely leaves the timer armed, which is the safe direction.
  Reproducing the real original shape — `clearTimeout` immediately after
  `fetch` — failed the test correctly.

- **The HTTP server leaked an `McpServer` per rejected request**, and never
  expired sessions at all. Both fixed; sessions now idle out after 30 minutes,
  swept on request rather than on a timer so nothing keeps the process alive.
  The leak is invisible from outside, so `openMcpSessions()` is exposed on the
  handle for the test — the same reasoning as `countLiveSupervisorsForTest`,
  and preferable to a test that reimplements the bookkeeping and pins its own
  copy.

- The `copy` size refusal named the parent of the project, and the `force`
  flag's schema description still promised the git-only refusal that the tool
  description had already been corrected about.

Also recorded: one run of the full suite failed `GET /health` with
`TypeError: fetch failed`, and passed on rerun and on every run since. Noted
as a flake rather than ignored; if it recurs it is a real defect in test
isolation between files.

Still open from that pass, unfixed: `.supervisors/` keeps one `spawn-<id>.log`
per supervisor ever spawned and `countLiveSupervisors` reads the directory on
every drain; cancelling an orphan that HAD started records "Cancelled before
it started"; `status.ts` renders an unroutable route as `ok` and prints
`quota=100%` for CLI routes that have no quota signal; `near-miss.ts` says a
key is "accepted and silently ignored" where the guard rejects it;
`leaderboard.ts` type-checks fetched values but not cached ones; `/mcp` has no
request-body limit; `client-register.ts` does a read-modify-write of
`~/.claude.json` with no lock; a mid-run `connect` failure across several
clients throws raw and leaves a temp file. None reproduced by the builder yet.

Suite: 1137 passed, 7 skipped, claims clean. Real dispatch log 425 throughout.

## Twenty-second pass (2026-09-02) — independent acceptor, verdict BLOCK

Run against `main` at 0d3e41b. The acceptor was told explicitly NOT to read
this file — the previous pass had correctly refused to certify its own
independence after being pointed at it. With the claims named in the prompt
instead, it lifted the cap honestly and the gate returned SHIP. It overrode to
BLOCK on two reproduced defects.

That briefing change is the single most valuable process fix of the series:
the acceptor found a data-destroying security defect on the first pass where
it was not reading the builder's own account of the work.

### B1 — SECURITY: a `copy` dispatch could delete files outside its workspace

Three defects composing, all in code written the previous day:

1. `secureProjectRoot` used `stat`, which FOLLOWS symlinks, so the uid it
   compared was the target's. An attacker plants a link at the predictable
   workspace path pointing at a directory the VICTIM owns, and the check
   passes against the victim's own uid.
2. `pruneStaleCopyWorkspaces` ran BEFORE the guard, so the one operation that
   deletes was never covered by it.
3. That sweep had no ownership, marker or name check at all — while its
   sibling `pruneAbandonedProjectRoots` has all three, under a comment saying
   "this deletes directories nothing else is watching".

Reproduced in a container as two real users: the victim's directory chmod'd to
0700, the project copied into it, and their files removed. A control run with
fresh mtimes isolated the deletion to the retention sweep while showing the
guard still failing.

Fixed: `lstat` and an outright refusal of a symlink (nothing this tool creates
is ever a link, and "follow it then validate" is how the first version was
defeated); `markProjectRoot` before the prune; `RUN_DIR_RE` on the sweep, the
same guard `pruneStaleJobs` already uses. Sabotage-checked on Linux at the
built artifact — `stat` follows and destroys, `lstat` refuses.

**The CHANGELOG entry shipped the previous day said a foreign-owned root was
"refused outright". That was false against the threat model it named, and it
was itself written to correct an earlier overclaim about the same guard.**
Three consecutive releases made a claim about this one guard; two were wrong.

### B2 — every non-ASCII character split across a read was corrupted

`buf.toString("utf8")` per chunk, so a multi-byte character straddling two
reads became replacement characters. Reproduced twice independently. This is
the path every MCP job uses; the buffered sibling concatenates before decoding
and was always correct, so the path only tests exercise looked right while
delivered work product was being mangled. Fixed with a `StringDecoder` per
stream, plus an `end()` flush at teardown so a child that dies mid-character
loses nothing silently.

### Also fixed this round

An aborted streaming request now stops the run (it ran to completion, spending
quota, with no jobId to cancel it — while OPERATIONS.md said "the run is lost
with the connection"); HTTP fanout with zero eligible routes refuses instead
of answering 200 `[]`; the buffered endpoint timeout now covers the body;
streaming error text is scrubbed of endpoint credentials; the 429 detector no
longer fires on line numbers or test assertions; hot-reload notices a
backwards mtime and documents its two inert cases; the `configure` snippet is
now the entry `connect` writes; and the omission notice added last round named
the wrong jobs when an id repeated.

### Mistakes made while fixing, recorded

- The duplicate-id test PASSED under sabotage: the assertion compared against
  the wrong count and could not have failed. Rewritten to pin the exact number
  and to assert that a rendered job is not named as omitted.
- Tightening the 429 separators broke an EXISTING test on `{"code": 429}` —
  the JSON form has a quote. Caught by the suite, not by me; the class now
  includes quotes and commas.
- A new test file was created at the wrong path — `tests/stream-subprocess.test.ts`
  rather than under `tests/streaming/` — so it ran zero tests while looking
  fine. (The claims checker flagged that path in this very sentence, since the
  file was moved; marked deliberate.) claims-check-ignore
- The fanout refusal initially swallowed the skip REASON, breaking a test whose
  whole point is that the reason is reported. The refusal now carries both the
  code in its prose and `skippedRoutes` structured, so a caller reading the old
  200 keeps its machine-readable answer.

### Verified TRUE by the acceptor

The leaderboard body timeout (8019ms, aborted); the MCP session leak fix (five
unknown-session POSTs → zero open); `retry_job` cancelling a derived orphan;
`configure` round-tripping `detect:`; `connect --remove`; workspace root modes
fresh and pre-existing.

### Reported clean

Boundary validation on both surfaces (an extensive matrix); route policy;
quota counters under 12 concurrent processes (300/300 exact); `doctor` and
`status` empty states; auth; HTTP teardown; no shell injection anywhere on the
dispatch path; `near-miss` typo distance checked exhaustively against a
reference implementation.

### Still open, NOT fixed

`.supervisors/` keeps one `spawn-<id>.log` per supervisor ever spawned;
breaker counts are lost under contention past ~24 concurrent processes while
the docblock claims they cannot be (exact at the shipped bound of 4); a slow
consumer can kill a healthy run and discard its result (`maxBufferedChunks`,
not reached at the shipped 1000); `scripts/fetch_benchmarks.py` replaces the
shipped 63-model file with ~18 bundled scores on any network failure and exits
0; `usage` empty state prints only a header; several `status.ts` display
issues; `site/` copy overstating what phones home and understating the tool
list; and assorted low items. None reproduced by the builder.

Suite after the fixes: 1152 passed, 8 skipped, claims clean. Real dispatch log
425 throughout, on both the pass and the fixes.
